// import { Router } from "express";
// import {
//   getMyClasses,
//   getClassStudents,
// } from "../controllers/class.controller";
// import { authenticateFaculty } from "../middlewares/auth.middleware";
// const router = Router();

// router.get("/my-classes", authenticateFaculty, getMyClasses);
// router.get("/:classId/students", authenticateFaculty, getClassStudents);

// export default router;
